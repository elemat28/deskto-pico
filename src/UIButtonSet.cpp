#include "UIButtonSet.h"

DeclaredButton UIButtonSet::RETURN = DeclaredButton("OS_RETURN",UIButton());
DeclaredButton UIButtonSet::SELECT = DeclaredButton("OS_SELECT",UIButton());
DeclaredButton UIButtonSet::NEXT   = DeclaredButton("OS_NEXT",  UIButton());

DeclaredButton fullSet::PREVIOUS = DeclaredButton("OS_PREVIOUS",  UIButton());
 